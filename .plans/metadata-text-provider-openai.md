# Plan

## Goal

Move tag, summary, and thumbnail-concept generation off `gemini-2.5-flash`,
which shuts down on 2026-10-16.

## Scope

- An OpenAI text client and provider dispatch in `scripts/generate_metadata.mjs`.
- `src/content/metadata.toml`.
- Tests, `.project/metadata.md`, `.project/build.md`, `.project/structure.md`.

## Non-goals

- The thumbnail image, already generated by `gpt-image-2`.
- Removing the Gemini client, which the dispatcher still reaches.

## Assumptions

- `gpt-5.6-luna` answers a strict JSON schema on the Responses API and accepts
  `reasoning.effort = "none"`.

## Steps

1. Add `openaiGenerateJson`, closing each schema for strict mode.
2. Carry `provider` on every request and dispatch on it, which is what
   `metadata.toml` already declares and nothing read.
3. Name `openai` and `gpt-5.6-luna` for tags and summaries.

## Verification

- `npx vitest run --project unit`
- `python3 scripts/verify.py --mode standard`
- Generate metadata for the article whose publication failed (job `99ee9ecf`)
  and read the tags and both summaries.

## Open Issues

None.
