# Article Metadata

Blog posts live in `src/content/posts/<post-id>/index.{ja,en}.md`.
Each Markdown file must start with TOML front matter delimited by `+++`.

Before publication, editor-managed Markdown and body images live under
`.drafts/posts/<post-id>/`, with images in its `assets/` directory. Saving a
draft does not change the published copy under `src/content/`. Markdown refers
to the build-stable URL `/content-assets/posts/<post-id>/<file>`; the editor
server resolves that URL from the draft first, then from published content.

Publishing validates both locales and promotes the complete draft directory,
including assets, to `src/content/posts/<post-id>/`. Production builds copy only
published assets. Do not move editor-managed images to `public/`. The `.drafts/`
tree is Git-ignored local state and is not backed up or synchronized.

About uses the same private-draft boundary but keeps its structured sources at
`.drafts/about/about.{ja,en}.toml` and `.drafts/about/news.{ja,en}.toml`.
Its selected avatar is stored in `.drafts/about/assets/` and referenced as
`/content-assets/about/about/<file>` until the complete About draft is published.

## Required Fields

- `title`: non-empty string.
- `date`: `YYYY-MM-DD` or `"auto"`.

`date = "auto"` is resolved by `node scripts/generate_metadata.mjs` from the git
commit that first added the Markdown file. The generated date is written back to
the file.

## Optional Fields

- `tags`: array of strings. Empty arrays are valid.
- `auto_tags`: table. `count`, when present, must be a positive integer.
- `kana`: reading of the title, used by the kana sort on the blog index.

Any other top-level field is rejected. Reading time is not a metadata field: it is
estimated per locale from the body by `src/readingTime.js` (600 CJK characters or
250 other words per minute, rounded up, minimum one).

`npm run new:post` scaffolds both files with a comment block listing the fields and
their accepted values. `scripts/generate_metadata.mjs` re-serializes the front matter,
so those comments are dropped the first time metadata is generated.

`auto_tags` is processed by `node scripts/generate_metadata.mjs`. Manual tags are
kept, and the generator adds the requested number of non-duplicate tags before
removing `auto_tags`.

## AI Provider Keys

Each section of `src/content/metadata.toml` names the `provider` and `model`
that answer it, and the generator reads the key that provider needs. Today
`[tag_generation]` and `[summary_generation]` name `openai` with
`gpt-5.6-luna`, and `[thumbnail_generation]` names `openai` with `gpt-image-2`,
so one key covers everything:

- `OPENAI_API_KEY` (billing enabled) for tags, summaries, thumbnail concepts,
  and thumbnail images.
- `GEMINI_API_KEY` for whichever text section names `provider = "gemini"`.

Text requests go to the Responses API with a strict JSON schema and reasoning
turned off; these are extraction tasks with one right shape, and a reasoning
model bills what it thinks as output.

The committed verification check does not call any AI provider; it only fails
when unresolved metadata remains. Store local keys through 1Password and expose
them through `op run`:

```sh
GEMINI_API_KEY=op://VAULT/ITEM/gemini_api_key
OPENAI_API_KEY=op://VAULT/ITEM/openai_api_key
```

Keep the references in `.op.env`; do not commit literal secret values. In GitHub
Actions, add `GEMINI_API_KEY` and `OPENAI_API_KEY` as Actions secrets only for
workflows that intentionally generate metadata.

## Automation Workflow

`.github/workflows/generate-metadata.yml` runs `npm run metadata:generate` on
pushes to `main` that touch `src/content/posts/**`, `src/content/metadata.toml`,
or `src/content/prompts/**` (and on manual dispatch). It injects both Actions
secrets, runs `python3 scripts/verify.py --mode ci`, then commits any resolved
frontmatter and generated thumbnails with `[skip metadata][skip translate]` so it
does not loop or re-trigger the translation workflow.

## AI Prompt Files

Prompt files live under `src/content/prompts/`. The text prompts are sent as
the request's system instruction:

- `src/content/prompts/tag-generation.md`
- `src/content/prompts/summary-generation.md`
- `src/content/prompts/thumbnail-concept.md` (derives the image concept from the
  Japanese title)

The image prompt is sent to OpenAI as the generation prompt:

- `src/content/prompts/thumbnail-generation.md` (contains the `{CONCEPT}`
  placeholder, replaced with the resolved concept)

Article title, locale, existing tags, known tags, and body are assembled by
`scripts/generate_metadata.mjs` as the user content for each text request.

## Summary

Use `[sumup]`.

```toml
[sumup]
mode = "text"
text = "Short summary."
```

Valid modes:

- `text`: requires `text` and is emitted as `post.summary`.
- `none`: emits an empty summary.
- `auto`: generated by Gemini and written back as `mode = "text"` with `generated = true`.

A summary may use `summary_generation.max_chars` characters, written or
generated. A model overshoots whatever count it is given, and an English
summary of a Japanese article runs long besides, so generation asks for 80% of
that limit and asks again for 65%, then 50%, while the answer overruns. Three
overruns fail with the length that came back.

## Thumbnail

Use `[thumbnail]`.

```toml
[thumbnail]
mode = "auto"
```

Valid modes:

- `none`: is written back as `mode = "file"` with the default path from `src/content/metadata.toml` and `generated = true`.
- `file`: requires `path`.
- `auto`: generates a thumbnail image and is written back as `mode = "file"` with
  `path = "/thumbnails/<post-id>.png"` and `generated = true`.

New posts scaffolded by `npm run new:post` default to `mode = "auto"`.

### Auto thumbnail generation

`mode = "auto"` generates one image per post id, shared by both locales:

- The image is stored at `public/thumbnails/<post-id>.png` and referenced as
  `/thumbnails/<post-id>.png`. Generation is idempotent: if the file already
  exists it is reused instead of regenerated. To have one drawn again, the
  editor's 「自動生成の画像を描き直す」 takes the stored image away and returns
  `[thumbnail]` to `mode = "auto"`; the next publication draws.
- The image subject (the `{CONCEPT}` placeholder in the image prompt) comes from
  an explicit `[thumbnail].concept` string when present, otherwise from the
  Japanese `title`. The Japanese title is always the concept source, so both
  locales share one image, every post has one whatever its `[sumup]` mode says,
  and the subject comes from the words the author chose for the particular
  thing the post is about.
- The image is generated by OpenAI `gpt-image-2` configured under
  `[thumbnail_generation]` in `src/content/metadata.toml` (`model`, `size`,
  `quality`, prompt files). Image generation requires a billing-enabled key.

The loader exposes the resolved value as `post.thumbnail`; rendering thumbnails
in the UI is a separate task.

## Validation

Run:

```sh
npx vitest run --project unit tests/check_frontmatter.test.mjs
```

To generate or refresh metadata:

```sh
npm run metadata:generate:op
```

To check that generated values are already written:

```sh
node scripts/generate_metadata.mjs --check
```

This check is static. It does not require `GEMINI_API_KEY`.

To inspect prompts without calling the API:

```sh
npm run metadata:preview-prompts
```

The full repository verification runs both the `frontmatter` and
`metadata_generate_check` phases from `.project/verification.toml`, followed by
metadata quality checks.
