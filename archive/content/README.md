# Archived repository content

A stored copy of articles, works, About, and generated thumbnails. Nothing
reads it — not the build, not the tests, not any workflow — so editing a file
here changes nothing.

The live content is in D1 and R2. Edit it through the editor, which writes
there through the author API.

See `.decisions/database-only-content-source.md`.
